%tr.comment{:id => dom_id(comment), :class => row_classes(comment) }
  %td= image_tag comment.user.photo.url(:admin)
  %td= comment.user.name
  %td= comment.deal.name
  %td= comment.body
  %td.date="#{time_ago_in_words comment.created_at} ago"
