%a{:href => user_url(@user), :style => ""}
  = image_tag @user.photo(:iphone_profile), :width => 150, :height => 150, :align => "left", :style => "margin-right: 10px; border-radius:10px"
= link_to("#{@user.best_name}", user_url(@user), :style => "text-decoration:none; font-size:14pt; color:gray; font-weight: bold")
%div{:style => "color:gray"}
  Loved your 
  = link_to(strip_emoji(@deal.name), deal_url(@deal), :style => "color:#FF7720; text-decoration: underline")
  post.